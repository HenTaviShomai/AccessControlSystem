services:
  # MySQL 数据库
  mysql:
    image: mysql:8.0
    container_name: access-control-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: access_control
      MYSQL_CHARACTER_SET_SERVER: utf8mb4
      MYSQL_COLLATION_SERVER: utf8mb4_unicode_ci
    ports:
      - "3306:3306"
    volumes:
      - mysql-data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    networks:
      - access-control-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-proot123"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis 缓存
  redis:
    image: redis:7-alpine
    container_name: access-control-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    networks:
      - access-control-network
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # 应用服务
  app:
    build: .
    container_name: acsystemJ25
    ports:
      - "8080:8080"
    # 修正：将启动参数更新为 Spring Boot 4.x 的 spring.data.redis 路径
    command: ["java", "-jar", "/app/app.jar", "--spring.data.redis.host=redis", "--spring.profiles.active=docker"]
    environment:
      SPRING_PROFILES_ACTIVE: docker
      # 修正：增加 allowPublicKeyRetrieval=true 以适配 MySQL 9.x 驱动
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/access_control?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: root123
      # 修正：添加符合 Spring Boot 4.x 规范的 Redis 环境变量
      SPRING_DATA_REDIS_HOST: redis
      SPRING_DATA_REDIS_PORT: 6379
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - access-control-network
    restart: unless-stopped

# 数据卷
volumes:
  mysql-data:
  redis-data:

# 网络
networks:
  access-control-network:
    driver: bridge