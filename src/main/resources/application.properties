spring.application.name=AccessControlSystem
spring:
    datasource:
        url: jdbc:mysql://localhost:3306/auth_system?useSSL=false & serverTimezone=Asia/Shanghai & characterEncoding=utf8
        username: root
        password: 152785
        driver-class-name: com.mysql.cj.jdbc.Driver

    redis:
        host: localhost
        port: 6379
        password:
        timeout: 5000ms

    jackson:
        time-zone: Asia/Shanghai
        date-format: yyyy-MM-dd HH:mm:ss

    server:
        port: 8080

    mybatis-plus:
    configuration:
l       og-impl: org.apache.ibatis.logging.stdout.StdOutImpl  # ??SQL??
    global-config:
        db-config:
i           d-type: auto